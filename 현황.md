# 작업 현황 (2026-08-13 오후 기준)

새 대화창에서 이 파일부터 읽으면 맥락이 잡힙니다.

---

## 프로젝트

멋쟁이사자처럼 14기 중앙 해커톤 / **SJF 트랙 (Fashion & Luxury with AI)** / MCM 브랜드 AS 서비스
나: 임규민 (백엔드 · AI 상담 챗봇). 팀: 기획 장동원, 백엔드 안광은·임규민, 프론트 세희·진성, 디자인 소연

**서비스명: Custodia (쿠스토디아)** — 라틴어 '수호·보관'. 8/12 회의 확정. (기존 'MCM 케어'·'CURA' 폐기)

---

## 🔴 일정 — 7일 남음

| 단계 | 일시 |
|---|---|
| **개발 마감** | **8/20(목)** — 팀 합의 |
| **제출 마감** | **8/21(금) 09:59:59** — 이후 수정 적발 시 즉시 탈락 |
| 서류 심사 | 8/21 10:00 ~ 8/24 → 트랙별 15팀 (총 60팀) |
| 트랙 피칭 | 8/25(화) 12:40 → 트랙별 2팀 (총 8팀) |
| 본선 토너먼트 | 8/25 16:00 → 최종 1팀 |
| 현장 | 8/25(화) COEX 마곡, 80개 대학 |

8/20~21은 어드민·마이페이지 추가 실험용 예비일 (회의 결정).

---

## 한 줄 상태

> **챗봇 완성 + 광은님 서버 계약에 맞춤 + Spring 연동 코드까지 완료.
> 남은 건 GitHub 등록과 배포. 그리고 OpenAI 결제(동원님 대기).**

---

## 심사 기준 (100점) — 내가 걸린 곳

| no | 기준 | 배점 | 내 몫 |
|---|---|---|---|
| 1 | 문제 정의 | 25 | 발표 재료 제공 (아래 '발견' 참고) |
| 2 | **실현 가능성** | **20** | "핵심 기능이 **실제 배포되어 작동**" ← **배포 안 하면 통째로 날아감** |
| 3 | 시장성 | 15 | — |
| 4 | UI/UX | 10 | — |
| 5 | 전달력 | 10 | 대본 낭독 시 **0점 처리** |
| 6 | **트랙 적합성** | **20** | 영역 정합성 10 (MCM 실제 자산) + 브랜드 톤 10 |

SJF 트랙 **Challenge 03 「360° 고객 경험」** — "발견부터 구매 이후까지 끊김 없는 연결". AS가 곧 '구매 이후'다.
요강 명시: **"AI는 모든 프로덕트에 필수"** / **"로그인 기능은 권장 사항이 아님"**

상세 요강은 프로젝트 문서 `claude/해커톤_요강_정리.md` 참고.

---

## 파일

| 파일 | 상태 | 내용 |
|---|---|---|
| `as_chatbot.py` | ✅ | 챗봇 본체 + FastAPI (약 1,650줄) |
| `spring_client.py` | ✅ | **광은님 서버 연동. 서버 없으면 더미로 폴백** |
| `mock_spring.py` | ✅ | **광은님 서버 흉내내는 가짜 서버 (연동 검증용)** |
| `as_dummy.json` | ✅ | AS 더미 4건 — 광은님 `AsStatus` 7단계 체계로 재구성 |
| `mcm_products.csv` | 🔄 | 상품 데이터 (8/13 재수집 중 → 750건 확보) |
| `mcm_crawler.py` / `collect_urls.py` | ✅ | 크롤러. **카테고리 6개 → 13개 전체**로 확장 |
| `chat.html` | ✅ | 테스트·시연용 채팅 화면 (실제 프론트 아님) |
| `Procfile` / `render.yaml` / `Dockerfile` | ✅ | 배포 설정 |
| `.env` / `.env.example` / `.gitignore` | ✅ | 키는 Git 제외됨 |
| `통합메모_광은님.md` | ✅ | 광은님 전달용 |
| `배포_가이드.md` | ✅ | git 커밋 → Render 배포 → 제출 체크리스트 |
| `시연_시나리오.md` | ✅ | 영상 2분 구성 + 피칭 질문 + 발표 재료 |
| `챗봇_해설.md` / `서버_이해하기.md` | ✅ | 구조·실행 설명 |
| `팀공유_챗봇_0812.md` | ⚠️ 갱신 필요 | 개인화·API 반영 안 됨 |

---

## 실행

```bash
cd ~/Desktop/mcm_crawler
uvicorn as_chatbot:app --reload           # 개발 (시연 땐 --reload 빼기)

MCM_MOCK=1 python as_chatbot.py --test    # 무료·무제한 검증 (평소 이걸로)
python as_chatbot.py --test               # 실제 GPT 24회 소모

python mock_spring.py                     # 가짜 Spring 서버 (포트 8080)
SPRING_API_URL=http://127.0.0.1:8080 uvicorn as_chatbot:app --port 8000
```

| 주소 | |
|---|---|
| `http://127.0.0.1:8000` | 일반 상담 (Frame 718) |
| `http://127.0.0.1:8000/?as_id=AS-2026-00871` | **개인화 상담 (Frame 717)** |
| `http://127.0.0.1:8000/docs` | API 문서 |

| 환경변수 | 용도 |
|---|---|
| `OPENAI_API_KEY` | 필수 |
| `MCM_MOCK=1` | GPT 미호출 (한도 절약) |
| `SPRING_API_URL` | 광은님 서버. **비우면 더미로 동작** |
| `SPRING_API_TOKEN` | 서버가 JWT 요구 시 |
| `ALLOWED_ORIGINS` | CORS. 비우면 전체 허용(개발용) |
| `SESSION_FILE` | 대화 이력 저장 경로 |

---

## 챗봇 구조

**RAG** — 질문 → 우리 자료 검색 → GPT에 [자료+질문] 전송 → 답변
GPT는 문장만 다듬고 내용은 전부 우리 데이터에서 나온다.

### 안전장치 6개

| # | 장치 |
|---|---|
| 1 | 자료 없으면 **GPT 호출 자체를 안 함** |
| 2 | 수선 금액 직답 금지 → 'AI 예상 견적'으로 유도 (견적 자료가 있으면 그 범위만 안내) |
| 3 | 개인정보 전송 전 정규식 치환 |
| 4 | 제품 여러 건이면 단정 금지 → 후보 나열 + 되묻기 |
| 5 | API 실패해도 서버 안 죽음 |
| 6 | 로그·에러에서 API 키 마스킹 |

### 검색 노이즈 대응 (8/13 추가)

상품을 125건 → 750건로 늘리자 **한 단어만 겹친 상품이 딸려오기 시작했다.**
"가방 지퍼가 고장났어요" → '가방' 하나 겹친 상품 14건이 동점 → 그중 2건이 GPT로.

→ **약한 점수(≤1) + 동점 다수(≥3)** 이면 상품 자료를 붙이지 않는다. (`WEAK_SCORE`, `TIE_LIMIT`)
점수 1이어도 동점자가 없으면(상품코드 일치) 그대로 쓴다.

---

## 광은님 백엔드(MCMcare_Back)와의 관계

**Java / Spring Boot.** 언어가 달라 한 프로세스에 못 넣는다 → **별도 포트로 띄우고 HTTP로 연결.**

### 맞춘 것

| 항목 | 광은님 기준 | 조치 |
|---|---|---|
| 접수번호 | `AS-%d-%05d` → `AS-2026-00871` (**5자리**) | 내 더미 변경 완료 |
| 진행 단계 | `AsStatus.TIMELINE` **7단계** | 내 타임라인 재구성 완료 |
| 단계 순서 | 수거 완료 → **접수 완료** | 내 것이 반대였음. 광은님이 맞음 |
| 손상 유형 | `DamageType` enum 6종 | `damage_type_code` 추가 |

**7단계**: 수거 완료 / 접수 완료 / 진단 및 견적 확정 / 수선 진행 중 / 품질 검수 / 반환 배송 / 완료

### 견적 — 계약이 이미 있다 ⭐

광은님이 "AI 모델 input/output을 몰라서 뺐다"고 했지만, 레포에 `EstimatePort` / `EstimateResult`가 이미 있다.

```java
EstimateResult { damageCategory, damageSeverity, confidenceGrade, confidenceNote,
                 items[] { repairItemName, minPrice, maxPrice } }
```

`StubEstimateAdapter`(규칙 기반)가 작동 중이고, 동원님은 **`ModelEstimateAdapter(@Profile("ai"))` 하나만** 만들면 된다.
챗봇 쪽 연동 코드는 완성됐다 — 견적이 있으면 금액을 근거 있게 설명하고, 없으면 기존대로 견적 화면으로 유도한다.

### 🔴 구멍 — 상품코드가 없다

`DetailResDto`에 `modelName`(이름)만 있고 **상품코드가 없다.** 챗봇은 코드로 소재·치수를 찾는다.
이름으로 역추적하는 폴백을 넣었지만, **수집 상품의 54%가 다른 상품과 이름을 공유**한다(한 이름을 12개가 쓰는 경우도 있음).
→ **광은님께 `modelCode` 필드 추가 요청 필요.**

---

## 개인화 (Frame 717)

`as_id`를 받으면 그 접수 건을 **항상 자료 맨 앞에** 넣는다. 첫 인사말은 GPT를 안 부르고 서버가 만든다.
고객 이름·멤버십 등급도 자료에 들어간다. **단 등급별 혜택은 확정 자료가 없어 말하지 않게 막아뒀다.**

| 접수번호 | 제품 | 단계 |
|---|---|---|
| AS-2026-00871 | Stark 사이드 스터드 비세토스 백팩 | 수선 진행 중 ← **시연 메인** |
| AS-2026-00914 | Aren 비세토스 슬링백 | 품질 검수 |
| AS-2026-00602 | Aren 디스코 비세토스 지퍼 지갑 | 반환 배송 |
| AS-2026-01033 | Tracy 비세토스 호보 | 픽업 예약 완료 · 수거 대기 중 |

---

## API (프론트 전달용)

```
POST   /chat/stream        { message, session_id, as_id }   ← 권장 (SSE)
POST   /chat               같은 요청, 한 번에 응답
DELETE /chat/{session_id}  대화 초기화
GET    /chat/opening?as_id= 첫 인사말 (GPT 미호출)
GET    /health             상태 + 지식베이스 건수 + Spring 연동 여부
GET    /docs               API 문서
GET    /                   chat.html
```

`/repairs`, `/repairs/{as_id}`는 광은님 `/api/asCase/*`와 겹친다 → **통합 시 삭제 예정.**

대화 이력은 재시작을 견디도록 파일에도 저장한다(`.sessions.json`).
무료 배포 플랜은 15분 놀면 서버가 잠들어 메모리가 날아가기 때문. **영구 보관이 아니다** (기획상 상담 내역 미저장).

---

## 🔴 남은 일

### 내 것

| # | 항목 | 기한 | 비고 |
|---|---|---|---|
| 1 | 크롤링 검증 + `MCM_MOCK=1 --test` | 오늘 밤 | |
| 2 | **GitHub 레포 등록** (`gyumin0847/MCMcare_Back_AI`, Public) | 오늘 | 제출 필수. 지금 코드가 맥 한 대에만 있음 |
| 3 | **배포** (Render → 8/18 이후 가비아) | ~8/17 | 제출 필수 + 심사 20점 |
| 4 | 시연 시나리오 확정 | ~8/18 | 초안 완료 |
| 5 | 광은님 서버와 실제 연결 | ~8/18 | 코드 완료, 주소 대기 |
| 6 | 발표 재료 동원님께 전달 | ~8/18 | |
| 7 | 배포 후 CORS 조이기 | 배포 직후 | `ALLOWED_ORIGINS` 설정 |
| 8 | `팀공유_챗봇_0812.md` 갱신 | 여유되면 | |
| 9 | 마이페이지·어드민 "딸깍" 실험 | 8/20~21 | 회의 결정 |

### 남에게 달린 것

| 항목 | 담당 | 안 되면 |
|---|---|---|
| **OpenAI $5 결제** | 동원 | **시연 중 챗봇이 429로 멈춘다** |
| 서버 주소·인증 방식 | 광은 | 실제 DB 연결 불가 |
| `modelCode` 필드 추가 | 광은 | 고객 제품의 소재·치수를 못 답함 |
| 수선 단가표 확정 | 동원 | 챗봇이 금액을 못 말함 |
| 멤버십 등급 필드 | 광은·동원 | 트랙 적합성 근거 약화 |
| 데모 대표 상품 확정 | 동원 | 목업 "뮌헨 백팩"이 실제 데이터에 없음 |
| **시연 영상 촬영 담당** | 미정 | **필수 제출물인데 담당자가 없다** |
| 로그인 제거 여부 | 광은 | 챗봇 인증 방식이 여기 걸림 |

---

## ⚠️ OpenAI — 원인 규명 완료 (8/13)

**조직이 Free tier다.** 하루 50회 / 분당 10회.

크레딧 $100이 있어도 안 풀린다. Limits 페이지 원문:

> "Your usage tier upgrades automatically when your total **credit purchases** reach each threshold."

**purchases — 결제한 금액** 기준이다. 받은 크레딧·카드 등록만으로는 안 올라간다.
(만약 $100이 구매분이었다면 Tier 3여야 하는데 Free다 → 프로모션 크레딧으로 보인다)

| | Free | Tier 1 ($5 결제) |
|---|---|---|
| 하루 | **50회** | 10,000회 |
| 분당 | **10회** | 500회 |

사용량: 74회에 **$0.01**. $5면 3만 회 넘게 쓴다. **`--test` 한 번에 24회 소모.**

규민은 아직 Owner가 아니다 — Billing·Limits 페이지가 권한 없음으로 막혀 있다.

---

## 결정된 것

- 서비스명 **Custodia**
- **로그인 제거** (요강 근거). 단 광은님 레포에 JWT가 아직 살아 있음
- 상담 내역 영구 저장 안 함 (Frame 718)
- 파인튜닝 대신 **RAG**
- 예약 변경·접수 취소·회원가입/탈퇴·어드민 = **범위 밖** (MVP)
- 어드민은 마지막에 디자인 없이 만들어 "시연 중 상태 업데이트"로 감점 리스크 제거 (시간 남으면)
- 견적 화면(712) 재진입: '나의 AS 상세'에 "AI 견적 다시 보기" 버튼 추가

---

## 이 프로젝트만의 발견 ⭐ (발표용)

### 사진으로도 이름으로도 구분되지 않는 상품이 실재한다

```
MMKEAVE12CO001   Stark 사이드 스터드 비세토스 백팩    16 x 33 x 41
MMKEAVE14CO001   스타크 사이드 스터드 비세토스 백팩    13 x 30 x 38   ← 한글 표기만 다름
MMKEAVE01K8001   Stark 비세토스 사이드 스터드 백팩              ← 단어 순서만 다름
```

색상·본체·트림·안감이 모두 같고 **치수만 다르다.**
**수집 상품의 54%가 다른 상품과 이름을 공유한다.** 한 이름을 12개 상품이 나눠 쓰기도 한다.

→ 그래서 챗봇이 **이름을 나열하지 않고 색상·크기·상품코드를 함께** 보여주고 되묻는다.
→ 이 문제는 **통합 지점에서도 재발했다** (`DetailResDto`에 코드가 없어 이름으로 특정 불가).

---

## 겪은 사고와 교훈

| 사고 | 원인 | 교훈 |
|---|---|---|
| 개인정보가 그대로 OpenAI로 나감 | 문서엔 "차단"이라 적혔지만 코드엔 없었음 | 문서를 믿지 말고 코드를 확인 |
| 13건 중 2건만 답하고 끝냄 | 검색이 상위 2건만 GPT에 넘김 | **LLM 탓하기 전에 내가 준 자료를 의심** |
| 상품 263건 중 138건이 중복 | 같은 상품이 여러 카테고리 경로로 접근 | URL이 다르다고 다른 데이터가 아님 |
| 목록 줄바꿈이 통째로 사라짐 | 정규식 `.`이 줄바꿈을 안 매칭 | `re.S` 필요 |
| `APIConnectionError` | 네트워크 아님. **키 형식 문제** | 겉 에러를 믿지 말고 `__cause__`를 볼 것 |
| 접수번호 자릿수가 팀과 달랐음 | 회의 구두 합의만 믿고 설정 파일을 안 봄 | **상대 레포의 설정 파일을 직접 확인** |
| 상품 확장하자 검색에 노이즈 | 125건에 맞춘 임계값을 그대로 씀 | 데이터가 늘면 기준도 다시 봐야 함 |

---

## 응답 규칙 (나에게)

- 결론부터. 서론·인사말 없이
- 표 적극 활용, 긴 문단 금지
- 나는 백엔드 초보 — 전문용어는 처음 나올 때 한 줄로 풀어줄 것
- 추측을 사실처럼 쓰지 말 것. 모르면 "확인 필요"라고 명시
- 코드는 실행 가능한 완성본으로. 파이썬/FastAPI. 한글 주석 필수
- 이미 조사한 것을 다시 조사하지 말 것 (이 파일과 `챗봇_해설.md` 먼저 확인)
