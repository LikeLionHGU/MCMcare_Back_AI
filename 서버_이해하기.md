# 서버 이해하기 — 내가 만든 걸 내가 설명하기 위한 문서

작성 2026-08-12 / 임규민
`챗봇_해설.md`가 "챗봇이 어떻게 답하는가"라면, 이 문서는 **"서버가 어떻게 도는가"**다.

---

## 1. "서버를 연다"가 무슨 뜻인가

```bash
uvicorn as_chatbot:app
```

이 한 줄이 하는 일은 **컴퓨터에 전화기를 놓고 전화를 받기 시작하는 것**이다.

| 서버를 켜기 전 | 서버를 켠 후 |
|---|---|
| `as_chatbot.py`는 그냥 텍스트 파일 | 8000번 포트에서 요청을 기다림 |
| 아무도 부를 수 없음 | 브라우저가 접속하면 응답함 |

> **포트(port)**: 한 컴퓨터 안에서 프로그램을 구분하는 번호. 아파트 동은 하나인데 호수가 여러 개인 것과 같다. 8000번은 우리 챗봇이 쓰는 호수.

---

## 2. 명령어 한 줄 해부

```
uvicorn  as_chatbot : app  --reload
   ①         ②      ③      ④
```

| | 뜻 |
|---|---|
| ① `uvicorn` | 전화를 받아주는 프로그램 (서버 프로그램) |
| ② `as_chatbot` | 파일 이름 (`as_chatbot.py`에서 `.py`를 뗀 것) |
| ③ `app` | 그 파일 안에 있는 `app = FastAPI(...)` 변수 |
| ④ `--reload` | 코드를 저장하면 서버를 자동 재시작 (**개발용**) |

**시연 때는 `--reload`를 빼야 한다.** 실수로 파일이 저장되면 서버가 재시작되면서 대화가 날아간다.

### 역할 분담

```
uvicorn   문지기   — 네트워크에서 요청을 받아 안으로 넘긴다
FastAPI   접수처   — 어느 함수로 보낼지 정하고, 데이터 형식을 검사한다
answer()  담당자   — 실제로 답을 만든다 (규민이 짠 부분)
```

uvicorn이 없으면 FastAPI는 아무 요청도 받을 수 없다. **둘은 경쟁 관계가 아니라 짝**이다.

---

## 3. 폴더 안 파일들이 하는 일

| 파일 | 역할 | 누가 읽나 |
|---|---|---|
| `as_chatbot.py` | **본체**. 챗봇 로직 + 서버 정의 | uvicorn |
| `chat.html` | 브라우저에 보여줄 화면 | 브라우저 |
| `mcm_products.csv` | 상품 750건 | as_chatbot.py |
| `as_dummy.json` | AS 접수 4건 (데모용 가짜) | as_chatbot.py |
| `mcm_crawler.py` | CSV를 만든 크롤러 | 사람이 직접 실행 |
| `collect_urls.py` | 크롤링할 URL 목록 수집 | 사람이 직접 실행 |

**서버가 켜질 때 한 번** CSV와 JSON을 읽어 메모리에 올린다.
그래서 **파일을 고치면 서버를 껐다 켜야 반영된다.** (`--reload`면 자동)

---

## 4. 요청 하나가 흐르는 길

고객이 "제 가방 어디에 있어요?"라고 쳤을 때:

```
1. 브라우저          POST /chat/stream 으로 요청 전송
                     { message: "제 가방 어디에 있어요?",
                       session_id: "web-a1b2", as_id: "AS-2026-00871" }
        ↓
2. uvicorn           요청 받음 → FastAPI에 넘김
        ↓
3. FastAPI           JSON을 파이썬 객체로 변환, 형식 검사
                     → chat_stream() 함수 호출
        ↓
4. _retrieve()       접수 건 조회 + 지식베이스 검색
        ↓
5. mask_pii()        전화번호·이메일 있으면 지움
        ↓
6. ask_gpt_stream()  OpenAI에 [자료 + 질문] 전송, 조각으로 받음
        ↓
7. polish_stream()   군더더기 문장 걸러냄
        ↓
8. FastAPI           SSE 형식으로 브라우저에 흘려보냄
        ↓
9. 브라우저          글자가 한 자씩 나타남
```

**4·5·7번이 규민이 설계한 부분이다.** 나머지는 도구가 해준다.

---

## 5. 주소(엔드포인트) 목록

서버를 켜면 이 주소들이 생긴다.

| 주소 | 하는 일 | 누가 씀 |
|---|---|---|
| `GET /` | 채팅 화면(`chat.html`) 제공 | 브라우저 |
| `POST /chat/stream` | 답변을 조각으로 흘려보냄 (권장) | 프론트 |
| `POST /chat` | 답변을 한 번에 반환 | 프론트 |
| `DELETE /chat/{session_id}` | 대화 초기화 | 프론트 |
| `GET /chat/opening?as_id=` | 상담 첫 인사말 (GPT 미호출) | 프론트 |
| `GET /repairs` | 나의 AS 목록 | 프론트 |
| `GET /repairs/{as_id}` | AS 상세 + 진행 이력 | 프론트 |
| `GET /health` | 서버 상태 확인 | 개발자 |
| `GET /docs` | **API 문서 (자동 생성)** | 프론트 |

> **엔드포인트**: 서버의 주소 하나하나. 식당 메뉴판의 항목 같은 것.

### `/docs`가 특별한 이유

FastAPI가 **코드를 읽어서 문서를 자동으로 만들어준다.**
버튼을 눌러 직접 호출해볼 수도 있다. 프론트에게 이 주소만 주면 된다.

```
http://127.0.0.1:8000/docs
```

---

## 6. 화면(`chat.html`)은 어떻게 만들어졌나

**HTML 파일 하나**다. CSS도 JavaScript도 그 안에 다 들어 있다.

```
chat.html
 ├─ <style>   말풍선 모양, 색, 배치
 ├─ <body>    헤더 / AS 정보 바 / 대화 영역 / 입력창
 └─ <script>  서버에 요청 보내고 답을 화면에 그리는 코드
```

### 핵심 동작 3가지

**① 세션 유지**

```javascript
const SESSION = "web-" + Math.random().toString(36).slice(2, 10);
```

페이지를 열 때 임의의 ID를 만들고, 매 요청에 같은 값을 보낸다.
→ 서버가 "같은 사람의 대화"로 인식해 앞 대화를 기억한다.

**② 접수 건 연결**

```javascript
const AS_ID = new URLSearchParams(location.search).get("as_id");
```

주소에 `?as_id=AS-2026-00871`을 붙이면 그 값을 읽어 서버에 함께 보낸다.
→ 실제 서비스에서는 '나의 AS 상세'에서 [이 AS 건 상담하기]를 누를 때 넘어온다.

**③ 스트리밍 수신**

서버가 보낸 조각을 받아 말풍선에 이어 붙인다.

```javascript
const reader = res.body.getReader();   // 응답을 조금씩 읽는다
...
b.textContent = full;                  // 받을 때마다 화면 갱신
```

이 부분이 프론트에게 제일 쓸모 있다. **SSE 파싱은 처음 하면 까다롭다.**

### 이건 진짜 프론트가 아니다

| `chat.html` | 백엔드 테스트·시연용 임시 화면 |
|---|---|
| 실제 화면 | 세희·진성이 소연 시안대로 구현 |

팀에 말할 때 **"프론트 만들었다"가 아니라 "테스트용 화면 만들었다"**고 해야 한다.

---

## 7. 서버 실행 방법 정리

```bash
cd ~/Desktop/mcm_crawler
export OPENAI_API_KEY=sk-...        # 터미널 닫으면 사라짐. 매번 필요

uvicorn as_chatbot:app --reload     # 개발할 때
uvicorn as_chatbot:app              # 시연할 때 (--reload 빼기)
```

접속 주소:

```
http://127.0.0.1:8000                            일반 상담
http://127.0.0.1:8000/?as_id=AS-2026-00871     개인화 상담
http://127.0.0.1:8000/docs                       API 문서
```

### 팀원도 접속하게 하려면 (같은 와이파이)

```bash
uvicorn as_chatbot:app --host 0.0.0.0
ipconfig getifaddr en0              # 내 IP 확인, 예: 192.168.0.12
```

팀원은 `http://192.168.0.12:8000` 으로 접속.

> ⚠️ `0.0.0.0`은 같은 와이파이의 **누구나** 접속 가능하다는 뜻이다.
> 카페·학교 공용 와이파이에서는 쓰지 말 것. 남이 내 API 키로 GPT를 쓸 수 있다.

### 서버 끄기

`Ctrl + C`

---

## 8. 자주 나는 오류

| 증상 | 원인 | 해결 |
|---|---|---|
| `can't open file` | 폴더가 다름 | `cd ~/Desktop/mcm_crawler` |
| `Address already in use` | 이전 서버가 안 죽음 | `lsof -ti:8000 \| xargs kill` |
| `OPENAI_API_KEY 환경변수가...` | 터미널을 새로 염 | `export` 다시 |
| `429 Rate limit` | 무료 한도(하루 50회) | 결제 수단 등록 |
| 브라우저에 아무것도 안 뜸 | 서버가 안 켜짐 | 터미널 확인 |
| 답변이 전부 "상담원 연결" | API 호출 실패 | 터미널의 `[GPT 호출 실패]` 확인 |

**터미널을 항상 보고 있어야 한다.** 오류는 브라우저가 아니라 터미널에 찍힌다.

---

## 9. 팀원에게 설명하는 법

### 프론트 (세희·진성)

> "서버 켜고 `/docs` 들어가면 API 문서가 자동으로 있어요.
> `session_id`는 프론트에서 만들어서 대화 내내 같은 값 보내주시면 되고,
> 상담 화면 들어올 때 `as_id`도 같이 주시면 그 접수 건을 아는 상태로 시작합니다.
> `chat.html`에 스트리밍 처리 예시 있으니 참고하셔도 돼요."

### 기획 (동원)

> "서버 하나 켜면 챗봇이 돌아가요. 상품 750건이랑 AS 안내 16건을 알고 있고,
> 접수 건 정보를 넘겨주면 그 건에 대해서도 답합니다.
> 실제 화면은 프론트가 만들 거고, 지금 건 제 테스트용이에요."

### 백엔드 (광은)

> "FastAPI로 돌리고 있고 엔드포인트는 9개예요.
> 접수 데이터는 지금 더미 JSON인데, 광은님 접수 API 붙으면
> 읽는 함수 한 개만 바꾸면 됩니다. (`load_repairs`)"

---

## 10. 용어 정리

| 용어 | 뜻 |
|---|---|
| **서버** | 요청을 받아 응답을 돌려주는 프로그램 |
| **포트** | 한 컴퓨터 안에서 프로그램을 구분하는 번호 (우리는 8000) |
| **uvicorn** | FastAPI 앱을 실제로 켜주는 프로그램 (ASGI 서버) |
| **FastAPI** | 요청을 함수에 연결해주는 파이썬 웹 프레임워크 |
| **엔드포인트** | 서버 주소 하나하나 (`/chat`, `/health`) |
| **GET / POST** | 조회 요청 / 보내는 요청 |
| **JSON** | 데이터를 주고받는 표준 형식 `{"키": "값"}` |
| **SSE** | 서버가 데이터를 조금씩 계속 밀어 보내는 방식 (스트리밍) |
| **CORS** | 브라우저가 다른 주소로의 요청을 막는 규칙. 프론트 연동 시 풀어야 함 |
| **127.0.0.1 / localhost** | "이 컴퓨터"를 뜻하는 주소. 남은 접속 못 함 |
| **환경변수** | 코드 밖에 두는 설정값. API 키를 여기 둬야 안전 |
