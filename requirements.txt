# 설치:  pip install -r requirements.txt

# 챗봇 본체
openai>=1.40            # GPT 호출
fastapi>=0.110          # API 서버
uvicorn[standard]>=0.29 # 서버 실행 (ASGI)
python-dotenv>=1.0      # .env 파일 읽기

# 크롤러
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0               # HTML 파서 (없으면 기본 파서로 동작)
