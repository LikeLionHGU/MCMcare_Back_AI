# 챗봇 해설 — 내가 만든 것을 내가 설명하기 위한 문서

작성 2026-08-12 / 임규민

이 문서 하나만 읽으면 `as_chatbot.py`가 무슨 일을 하는지, 왜 그렇게 짰는지,
그리고 발표에서 뭐라고 말해야 하는지가 잡힌다.

---

## 1. 한 문장 요약

> **고객 질문이 오면 우리 자료에서 관련 부분을 먼저 찾고, 그 자료만 GPT에게 주면서
> "이걸로만 답해"라고 시키는 챗봇.**

GPT는 문장을 다듬는 역할만 한다. **내용은 전부 우리 자료에서 나온다.**
이 방식을 **RAG(Retrieval-Augmented Generation, 검색 증강 생성)** 라고 부른다.

### 왜 그냥 GPT한테 물어보면 안 되나

| 그냥 GPT | 우리 방식 (RAG) |
|---|---|
| MCM 상품 정보를 모름 | CSV 24건을 자료로 줌 |
| 모르면 그럴듯하게 **지어냄** (환각) | 자료 없으면 **아예 호출 안 함** |
| "수선비 10만원쯤" 지어낼 수 있음 | 금액 관련 자료에 "금액 안내 금지"가 박혀 있음 |
| 회사 정책이 바뀌면 모델을 다시 학습 | 자료(CSV·안내문)만 고치면 끝 |

**발표에서 이 표가 핵심이다.** "왜 파인튜닝 안 했냐"는 질문의 답이기도 하다.

---

## 2. 전체 흐름

```
고객: "사이드 스터드 백팩 치수 알려줘"
   │
   ├─① 개인정보 있나? ─────────→ 있으면 [전화번호] 등으로 치환
   │
   ├─② 우리 자료 32건에서 검색
   │     AS 안내 8건 + 상품 24건
   │     → 5건 일치 발견
   │
   ├─③ 자료가 하나도 없으면?  ──→ 여기서 끝. GPT 호출 안 함.
   │                              "상담원 연결을 도와드릴까요?"
   │
   ├─④ 찾은 자료 + 직전 대화 + 질문을 GPT에 전송
   │
   └─⑤ 답변 + "어떤 자료로 답했는지(근거)" 함께 반환
```

**③번이 이 챗봇의 핵심 방어선이다.** 자료가 없으면 GPT를 부르지 않는다.
호출을 안 하면 지어낼 수가 없다. 프롬프트로 부탁하는 게 아니라 구조가 막는다.

---

## 3. 파일 구조

| 파일 | 역할 |
|---|---|
| `as_chatbot.py` | 챗봇 본체 (검색 + GPT 호출 + 서버) |
| `chat.html` | 브라우저 채팅 화면 |
| `mcm_products.csv` | 크롤러가 모은 상품 24건 — 챗봇이 자동으로 읽음 |
| `mcm_crawler.py` | 위 CSV를 만든 크롤러 |

`as_chatbot.py` 안의 7개 섹션:

| # | 섹션 | 하는 일 |
|---|---|---|
| 1 | AS 지식베이스 | 기획 화면 문구 + MCM 공식 관리법 **8건** (사람이 직접 작성) |
| 2 | 상품 지식베이스 | CSV를 읽어 상품 **24건**을 자료로 변환 |
| 3 | 검색 | 질문과 관련된 자료 고르기 |
| 4 | GPT 호출 | 개인정보 마스킹 → API 호출 → 실패 처리 |
| 5 | 상담 로직 | 안전장치를 적용해 최종 답변 만들기 |
| 6 | FastAPI 서버 | 프론트가 부를 수 있는 API + 채팅 화면 제공 |
| 7 | 터미널 테스트 | `--test` 자가 점검, 대화 모드 |

---

## 4. 검색은 어떻게 하나 (벡터DB 안 쓴 이유)

**방식: 키워드 매칭.** 자료마다 키워드 목록을 붙여두고, 질문에 그 단어가 있으면 점수 +1.

```python
score = sum(1 for k in keywords if k in question)
```

> **벡터DB / 임베딩**: 문장을 숫자 배열로 바꿔 "의미가 비슷한 것"을 찾는 방식.
> 자료가 수천 건일 때 쓴다.

**우리는 32건이다.** 벡터DB를 쓰면 설치·인덱싱·비용이 늘고 디버깅이 어려워진다.
32건에서는 키워드 매칭이 더 빠르고, 무엇보다 **왜 이 자료가 걸렸는지 눈으로 확인된다.**

> 발표에서 "왜 벡터DB 안 썼나요?" → **"32건 규모에서는 과한 설계라고 판단했습니다.
> 데이터가 수백 건을 넘으면 임베딩 방식으로 바꿀 생각입니다."**
> (모른다고 안 쓴 게 아니라, 판단해서 안 썼다는 게 요점)

### 검색에서 겪은 문제 3개

**문제 1 — 상품이 AS 안내를 밀어냄**

상품이 24건이라 "비세토스 캔버스 관리법" 질문에 상품만 잔뜩 걸리고
정작 관리법 안내가 밀려났다.
→ **해결**: AS 안내와 상품을 **따로** 뽑아서 합친다. (`search()`)

**문제 2 — 후보 일부를 전체인 것처럼 답함** ⭐ 제일 중요

"비세토스 백팩 얼마?" → 실제 13건인데 챗봇이 **2건만 말하고 끝냈다.**
상위 2건만 GPT에 넘겼으니 GPT는 2건만 답할 수밖에 없었다. GPT 잘못이 아니다.

→ **해결**: 일치 상품이 여러 건이면 **건수·가격범위·전체 목록**을 요약해서 같이 넘긴다.
지금은 이렇게 답한다.

```
총 8건이 있으며, 판매가는 1,750,000원에서 2,150,000원입니다.
정확한 상품을 확인하기 위해 상품코드를 알려주시면...
```

**문제 3 — 단어 하나만 겹쳐도 후보에 낌**

"비세토스 백팩"에 '백팩'만 겹친 *모노그램 레더 백팩*까지 껴서 18건이 되고
가격 범위가 엉뚱해졌다.
→ **해결**: **최고 점수와 같은 것만** 후보로 인정. 18건 → 8건으로 정정됐다.

---

## 5. 안전장치 6개 — 발표의 핵심

| # | 장치 | 어떻게 |
|---|---|---|
| 1 | 자료 없으면 **GPT 호출 자체를 안 함** | `if not hits: return _no_hit_answer(...)` |
| 2 | 수선 금액 직답 금지 | 자료에 "금액 안내 금지"를 넣고 'AI 예상 견적'으로 유도 |
| 3 | 개인정보를 GPT로 안 보냄 | 정규식으로 전화·이메일·주민·카드·주소 치환 |
| 4 | 여러 건이면 단정 금지 | 건수·전체 목록을 넘기고 되묻게 함 |
| 5 | API 실패해도 서버 안 죽음 | `try/except` → 상담원 안내로 대체 |
| 6 | 로그에 API 키 안 남김 | `redact()` — `sk-***REDACTED***` |

### 3번을 강조해야 하는 이유

처음엔 문서에 "개인정보는 GPT로 안 보냄"이라고 적혀 있었지만
**코드에는 그 기능이 아예 없었다.** 고객이 채팅창에 전화번호를 치면 그대로 나갔다.

```python
# 지금은 이렇게 막는다
PII_PATTERNS = [
    (re.compile(r"[\w.+-]+@[\w-]+\.[\w.]+"), "[이메일]"),
    (re.compile(r"(?<!\d)01[016-9][-\s.]?\d{3,4}[-\s.]?\d{4}(?!\d)"), "[전화번호]"),
    ...
]
```

패턴 **순서가 중요하다.** 카드번호를 먼저 지워야 전화번호 규칙이
카드번호 가운데를 잘라먹지 않는다. (`1234-5678-9012-3456` → `1[전화번호]-3456` 사고를 실제로 겪음)

그리고 **치수·가격·상품코드는 안 가려지는지** 확인했다.
`16 x 33 x 41`, `2150000`, `MMKEAVE12CO001` 모두 통과.

---

## 5-B. 자료를 못 찾았을 때 — 거절도 기능이다

처음엔 **무슨 질문이든 똑같이** "상담원 연결을 도와드릴까요?"로 답했다.
그런데 "오늘 날씨 어때?"에 상담원을 연결하겠다는 건 어색하다. 4갈래로 나눴다.

**판정 순서가 중요하다. 위에서부터 걸린다.**

| 순서 | 조건 | 답변 |
|---|---|---|
| 1 | 인사·감사 ("안녕", "고마워", "ㅎㅎ") | 짧은 인사 |
| 2 | **환불·반품·교환·매장·분실** 등 (`REFER_TO_AGENT`) | 상담원 연결 1600-1976 |
| 3 | 자료를 찾음 | GPT 호출 |
| 4 | 못 찾았지만 AS 얘기 (`SERVICE_HINTS`) | **기본 안내(접수·기간·견적)를 주고 GPT가 답** |
| 5 | 그 외 | "AS 상담만 도와드릴 수 있어요" + 예시 4개 |

**2번을 검색보다 먼저 두는 이유.** "반품하고싶어"를 검색에 넘기면
`하고싶`이 접수 안내 키워드에 걸려서 **반품 문의에 AS 접수 절차를 안내한다.**
답할 수 없는 것은 검색 전에 걸러내야 한다.

**4번이 대화를 살린다.** 예전엔 이것도 상담원으로 보냈는데,
"내 가방을 수선 맡기고 싶은데" 같은 정상적인 문의까지 끊겨버렸다.

### 여기서 배운 것 ⭐

> **키워드를 내가 상상해서 쓰면 반드시 빠진다.**

처음 지식베이스를 만들 때 `소요 기간`의 키워드에 "기간, 얼마나, 며칠…"을 넣었다.
그런데 **`수선`이라는 단어를 어디에도 넣지 않았다.** 그 결과:

```
"내 가방을 수선 맡기고 싶은데"  → 검색 0건 → 상담원 연결   ❌
```

AS 서비스 챗봇인데 "수선"을 못 알아들었다.
지금은 접수 방법 키워드에 실제 고객 표현을 40개 넘게 넣었다.

```
수선 · 수리 · 고치 · 맡기 · 보내 · 의뢰 · 하고싶 · 하려면
찢어 · 터졌 · 지퍼 · 손잡이 · 스트랩 · 긁힘 · 오염 · 변색 · 벗겨 …
```

> 발표에서 쓸 말: **"실제 문장으로 테스트해보니 제가 상상한 키워드가 고객 표현과 달랐습니다.
> 그래서 손상 유형·구어체 표현을 키워드에 반영했습니다."**

### 요약은 상품 질문에만 붙인다

"가죽이 벗겨졌는데 고칠 수 있나요?"에 `가죽`이 상품 17건에 걸려
**"총 17건입니다"**로 답하던 문제가 있었다.
치수·가격·종류를 묻는 질문일 때만 요약을 붙이도록 제한했다. (`PRODUCT_QUERY_WORDS`)

**엉뚱한 질문에 GPT를 부르지 않는 이유**는 세 가지다.

1. 명품 브랜드 챗봇이 날씨·정치·잡담에 답하면 브랜드 리스크가 된다
2. 환각의 여지를 남긴다
3. 비용이 든다

> 발표에서 "범위 밖 질문은 왜 안 받나요?" → **"답을 잘 거절하는 것도 기능으로 봤습니다.
> 대신 거절할 때 무엇을 할 수 있는지 알려주게 만들었습니다."**

---

## 5-C. 스트리밍 — "생각하는" 느낌

미리 써둔 문장은 **즉시** 나온다. 빠른 게 아니라 아예 생각을 안 한 것이고,
그래서 기계처럼 느껴진다.

**해결: 스트리밍.** OpenAI에 `stream=True`를 주면 답변을 완성해서 한 번에 주지 않고
**만들어지는 대로 조금씩** 보내준다. 그걸 그대로 브라우저로 흘려보내면
챗GPT처럼 글자가 한 자씩 나타난다.

```
브라우저 ──POST /chat/stream──> 서버 ──stream=True──> OpenAI
        <──"수선 "──                 <──"수선 "──
        <──"소요 "──                 <──"소요 "──
        <──"기간은"──                 ...
```

전송 형식은 **SSE(Server-Sent Events)** — 서버가 한 줄씩 계속 밀어 넣는 방식이다.

```
data: {"type":"meta",  "sources":["소요 기간"], "used_gpt":true, ...}
data: {"type":"delta", "text":"수선 "}          ← 여러 번 반복
data: {"type":"done"}
```

`meta`를 **먼저** 보내는 이유: 근거 태그를 답변보다 먼저 확보해두면
답변이 끝나는 순간 바로 붙일 수 있다.

미리 써둔 문장(범위 안내 등)도 6글자씩 조각내 보낸다.
GPT를 부르지 않았는데도 화면상 자연스럽게 흘러나온다.

> ⚠️ 기존 `POST /chat`도 그대로 남겨뒀다. 프론트가 스트리밍 처리가 부담스러우면
> `/chat`을 쓰면 된다. 둘 다 같은 로직(`_retrieve`)을 쓰므로 답변 내용은 동일하다.

---

## 5-D. 말투 — 내부 사정을 손님에게 말하지 않기

챗봇이 어색하게 느껨지는 가장 큰 원인은 **내용이 아니라 말투**였다.

| 어색한 표현 | 왜 문제인가 | 바꾼 말 |
|---|---|---|
| "제가 가진 자료로 확인이 어렵습니다" | '자료'는 우리 내부 사정이다 | "이 부분은 상담원이 직접 확인해 드리는 게 정확해요" |
| "총 8건 일치합니다" | 검색 시스템 용어 | "말씀하신 제품은 여덟 가지가 있어요" |
| "참고 자료에 따르면" | 프롬프트 구조가 새어나온 것 | (그냥 답한다) |
| "AI로서 답변드릴 수 없습니다" | 손님은 AI 사정에 관심 없다 | "여기서는 금액까지 안내가 어려워요" |

**원칙: 무엇을 못 하는지 말하지 말고, 어디서 되는지 알려준다.**

프롬프트에 금지 표현을 명시하고, `LEAK_WORDS`로 답변에 새는지 자동 검사한다.

```python
LEAK_WORDS = ["제가 가진 자료", "참고 자료", "자료에 없", "지식베이스",
              "데이터베이스", "검색 결과", "건 일치", "AI로서", "GPT", ...]
```

`--test`에서 이 표현이 답변에 나오면 🔴 경고가 뜬다.
**프롬프트로 부탁만 하고 끝내지 않고, 어겼는지 검사까지 붙인 것**이 요점이다.

### 미리 써둔 문장도 종류별로 나눴다

같은 문장을 돌려쓰면 금방 기계처럼 느껨진다. 문의 종류별로 다르게 답한다.

| 문의 | 답변 |
|---|---|
| 환불·교환 | "구매하신 경로와 결제 상태를 함께 확인해야 해서…" |
| 매장 위치 | "MCM 공식 홈페이지나 고객센터에서 확인하실 수 있어요" |
| 분실·도난 | "확인이 필요한 부분이라 상담원이 직접 도와드려야 해요" |
| 첫인사 | "안녕하세요, MCM 케어입니다. 어떤 점이 궁금하신가요?" |
| 마무리 인사 | "도움이 되었다면 다행이에요. 필요할 때 언제든 다시 찾아주세요." |

첫인사와 마무리 인사를 구분한 이유: **"고마워요"에 "안녕하세요"로 답하면 어색하다.**

### 군더더기는 코드로 잘라낸다 ⭐

프롬프트에 "쓰지 마"라고 써도 GPT는 계속 붙였다.

```
"…예상 수선 비용 범위를 확인하실 수 있습니다. 그렇게 해보시면 좋을 것 같아요."
                                            └─ 정보량 0. 이미 방법을 알려줬는데 또 "그렇게 하세요"
```

**프롬프트는 부탁이고 강제가 아니다.** 그래서 코드로 처리했다.

```python
FILLER_PATTERNS = [
    r"^그렇게\s*(해\s*보시|하시)",     "좋을\s*것\s*같(아요|습니다)\s*[.!]?\s*$",
    r"^추가(로)?\s*(궁금|문의)",       r"^도움이\s*(되셨|되었)",
    r"^언제든\s*(문의|말씀)",          r"^참고(하시|해\s*주)",  ...
]
```

| 함수 | 쓰는 곳 |
|---|---|
| `polish(text)` | 완성된 답변에서 군더더기 문장 제거 (`/chat`) |
| `polish_stream(gen)` | 스트리밍 중 **문장이 완성될 때마다** 검사해 걸러 보냄 (`/chat/stream`) |

스트리밍이 까다로웠다. 조각이 7글자씩 오는데 문장 단위로 판단해야 하므로,
버퍼에 모아두고 `_cut_sentence()`로 완성된 문장만 꺼내 검사한다.

**안전장치 2개**

1. 목록 줄(`·`로 시작)은 건드리지 않는다 — 제품 나열이 깨지면 안 된다
2. 문장이 **전부** 군더더기로 판정되면 원문을 그대로 둔다 — 빈 답변 방지

> 발표에서 쓸 말: **"프롬프트로 규칙을 주되, 지켜졌는지 코드로 검사하고
> 어기면 후처리로 고칩니다. LLM 출력을 그대로 신뢰하지 않는 구조입니다."**

---

## 6. 연속 대화는 어떻게 되나

HTTP는 원래 **매 요청이 독립적**이다. 서버는 이전에 무슨 말을 했는지 모른다.

```
고객: 사이드 스터드 백팩 치수 알려줘
챗봇: 5건입니다. 상품코드를 알려주시면...
고객: 12CO001이요
챗봇: ???        ← "12CO001"만으로는 아무것도 모른다
```

**해결 2가지를 같이 썼다.**

**(1) 서버가 대화를 기억한다** — `session_id`

프론트가 같은 `session_id`를 계속 보내면, 서버가 그 대화의 최근 3턴을 저장해뒀다가
GPT에 같이 넘긴다.

```python
SESSIONS: dict[str, list] = {}   # 세션ID → 대화 목록
```

> ⚠️ **서버 메모리에만 저장한다. 서버를 끄면 사라진다.**
> 데모에는 충분하지만 실서비스라면 DB나 Redis로 옮겨야 한다.
> 이 한계를 알고 있다는 것 자체가 발표에서 점수가 된다.

**(2) 부분 상품코드도 알아듣는다**

고객은 `MMKEAVE12CO001`을 다 치지 않는다. "12CO001이요"라고 한다.
질문에서 영숫자 덩어리를 뽑아 **상품코드의 일부인지** 따로 본다. (부분 일치 0.5점)

**(3) 맥락이 필요할 때만 이전 대화를 붙인다**

상품 자료가 안 잡혔을 때만 직전 발화를 붙여 재검색한다.
처음부터 붙이면 새 주제로 넘어가도 이전 주제가 계속 따라붙는다.

| 대화 | 결과 |
|---|---|
| "치수 알려줘" → "12CO001이요" | ✅ 해당 상품 치수 |
| "MMKEAVE12CO001 크기" → "그거 얼마예요?" | ✅ 그 상품 가격 (이전 대화 참고) |
| "수선 기간?" → "접수 준비물?" | ✅ 새 주제로 정상 전환 (맥락 안 끌고 옴) |

---

## 7. 이 프로젝트만의 발견 ⭐

8/10에 크롤링 데이터를 뜯어보다 찾은 것:

> **사진 한 장으로는 구분이 불가능한 상품 쌍이 존재한다.**

| | MMKEAVE12CO001 | MMKEAVE14CO001 |
|---|---|---|
| 색상·본체·트림·안감 | 모두 동일 | 모두 동일 |
| 치수 | 16 x 33 x 41 | 13 x 30 x 38 |

사진으로 얻을 수 없는 **치수만** 다르다. AI 이미지 매칭 점수가 소수점까지 같아진다.
→ **AI가 단정하면 안 되고, 후보를 제시해야 한다**고 프론트·기획에 공유했다.

**그리고 챗봇에서 실제로 그렇게 구현했다.**

```
고객: 사이드 스터드 백팩 치수 알려줘
챗봇: 조건에 맞는 상품이 총 5건입니다.
      1. Stark 사이드 스터드 비세토스 백팩 [MMKEAVE12CO001] — 16 x 33 x 41 cm
      2. Stark 사이드 스터드 비세토스 백팩 [MMKEAVE12BK001] — 16 x 33 x 41 cm
      3. Stark 비세토스 사이드 스터드 백팩 [MMKGSVE01K8001] — 16 x 33 x 43 cm
      4. 스타크 사이드 스터드 비세토스 백팩 [MMKEAVE14CO001] — 13 x 30 x 38 cm
      5. 스타크 사이드 스터드 비세토스 백팩 [MMKEAVE14BK001] — 13 x 30 x 38 cm

      어떤 상품의 치수를 확인하고 싶으신가요? 상품코드를 말씀해 주시면...
```

**데이터를 직접 뜯어봤기 때문에 나온 설계다.** 발표에서 반드시 말할 것.

---

## 8. 실행법

```bash
cd ~/Desktop/mcm_crawler

# 눈에 보이는 채팅 화면 (추천)
export OPENAI_API_KEY=sk-...
uvicorn as_chatbot:app --reload
# → 브라우저에서 http://127.0.0.1:8000

# API 문서 (프론트에게 이 주소를 주면 된다)
# → http://127.0.0.1:8000/docs

# 터미널 대화
python as_chatbot.py

# 자가 점검 11케이스 + 연속 대화 2케이스
python as_chatbot.py --test
MCM_MOCK=1 python as_chatbot.py --test    # 돈 안 쓰고 검색만
```

### API 명세 (프론트 전달용)

```
POST /chat/stream          ← 화면에 글자가 흘러나오게 (권장)
  요청 : { "message": "...", "session_id": "abc123" }
  응답 : SSE 스트림. 한 줄씩 아래 JSON이 온다
     data: {"type":"meta","sources":[...],"used_gpt":true,"masked":0,"used_history":false}
     data: {"type":"delta","text":"수선 "}      ← 여러 번 반복
     data: {"type":"done"}

POST /chat                 ← 한 번에 받기 (스트리밍이 부담스러우면 이쪽)
  요청 : { "message": "수선 얼마나 걸려요?", "session_id": "abc123" }
  응답 : {
    "answer": "수선 소요 기간은...",
    "sources": ["소요 기간", "견적 및 비용"],   ← 근거 (디버깅·신뢰성 표시용)
    "used_gpt": true,
    "masked": 0,              ← 개인정보 가린 건수
    "used_history": false,    ← 이전 대화를 참고했는지
    "session_id": "abc123"
  }

DELETE /chat/{session_id}   대화 초기화
GET    /health              서버 상태 + 지식베이스 건수
```

**`session_id`는 프론트가 만들어서 대화 내내 같은 값을 보내면 된다.**
안 보내면 매번 새 대화로 처리된다(=기억 없음).

---

## 9. 겪은 오류와 원인 (다시 안 밟으려고)

| 증상 | 진짜 원인 | 교훈 |
|---|---|---|
| `python: can't open file` | 홈 폴더에서 실행함 | `cd`로 폴더 이동 먼저 |
| `APIConnectionError: Connection error.` | 네트워크 문제 **아니었음** | 겉 에러 메시지를 믿지 말 것 |
| `└ 원인: LocalProtocolError` | 요청이 나가기도 전에 HTTP 헤더 생성 실패 = **키 형식 문제** | `__cause__`를 출력해야 진짜 원인이 보인다 |
| 상품 13건 중 2건만 답함 | 검색이 2건만 넘김 | **GPT를 의심하기 전에 내가 준 자료를 의심하라** |

마지막 줄이 이번 작업에서 제일 큰 교훈이다.
LLM이 이상하게 답하면 대부분 **모델이 아니라 넣어준 자료가 문제**다.

---

## 10. 용어 정리

| 용어 | 뜻 |
|---|---|
| **RAG** | 검색 증강 생성. 자료를 먼저 찾아서 그것만 보고 답하게 하는 방식 |
| **환각(Hallucination)** | LLM이 모르는 걸 그럴듯하게 지어내는 현상 |
| **지식베이스** | 챗봇이 참고하는 우리 자료 모음. 여기선 32건 |
| **프롬프트** | 모델에게 주는 지시문. `SYSTEM_PROMPT`가 규칙 7개 |
| **temperature** | 0에 가까울수록 자료에 충실. 우리는 0.2 |
| **엔드포인트** | 서버 주소 하나하나. `/chat`, `/health` 등 |
| **CORS** | 브라우저가 다른 주소로의 요청을 막는 규칙. 프론트 연동 시 풀어줘야 함 |
| **세션** | 한 고객의 대화 한 묶음. `session_id`로 구분 |
| **미들웨어** | 요청이 들어와 처리되기 전/후에 공통으로 끼어드는 코드 |
| **스트리밍** | 답변을 완성해서 한 번에 주지 않고 만들어지는 대로 조금씩 보내는 것 |
| **SSE** | 서버가 클라이언트에 데이터를 계속 밀어 넣는 방식. 스트리밍에 사용 |
| **제너레이터** | `yield`로 값을 하나씩 내놓는 파이썬 함수. 스트리밍 구현에 사용 |

---

## 11. 아직 안 된 것 (숨기지 말 것)

| 항목 | 상태 |
|---|---|
| 환불·반품 문의 | ❌ 정책 자료가 없어 상담원 연결로만 처리. **기획자에게 문서 요청 필요** |
| 대화 저장 | ⚠️ 서버 메모리만. 재시작하면 사라짐 |
| 사진 업로드 | ❌ 미구현 (AI 예상 견적은 동원님 담당) |
| 실제 AS 접수 연동 | ❌ 챗봇은 안내만. 접수는 별도 API (광은님 명세서) |
| 어드민 | ❌ 회의에서 제외 결정 (더미 데이터로 대체) |

**발표에서 "안 된 것"을 먼저 말하면 신뢰를 얻는다.**
특히 환불 건은 "자료가 없어서 못 하는 게 아니라, **없는 걸 지어내지 않도록 일부러 막았다**"로
설명하면 오히려 설계 강점이 된다.
