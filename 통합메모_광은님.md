# 챗봇 ↔ MCMcare_Back 통합 메모

작성: 임규민 · 2026-08-13
대상: 안광은
챗봇: `as_chatbot.py` (Python / FastAPI)
서버: `LikeLionHGU/MCMcare_Back` (Java / Spring Boot, `ef7b74e` 기준)

---

## 0. 결론

레포 보고 **제 쪽을 광은님 계약에 맞췄습니다.** 아래 3개는 이미 반영 완료고, 광은님이 하실 일은 **어댑터 한 개**입니다.

| | 항목 | 상태 |
|---|---|---|
| ✅ | 접수번호 `AS-%d-%05d` (5자리) | 제 더미를 `AS-2026-00871` 형태로 변경 |
| ✅ | `AsStatus` 7단계 label·순서 | 제 타임라인을 `TIMELINE` 그대로 재구성 |
| ✅ | `DamageType` enum 6종 | 제 더미에 `damage_type_code` 추가 |
| 🔴 | 챗봇 ↔ DB 연결 | 아래 2장 |

**언어가 달라서(Java vs Python) 한 프로세스에 못 넣습니다.** 챗봇은 별도 포트로 띄우고, 프론트가 직접 호출하거나 Spring에서 프록시하는 구조가 됩니다.

---

## 1. 제가 맞춘 것

### 접수번호

`application.yaml`의 `as-format: "AS-%d-%05d"`를 따랐습니다.

```
변경 전  MCM-2026-000871   (6자리)
변경 후  AS-2026-00871     (5자리)
```

`NumberGenerator` 주석에 "스토리보드에 AS-YYYY-NNNNN과 MCM-YYYY-NNNNNN이 혼재"라고 쓰여 있던데, **8/12 회의에서 `AS-`로 통일하기로 확정**됐습니다. 지금 설정 그대로 가시면 됩니다.

### 진행 단계

`AsStatus.TIMELINE`의 7단계를 label·순서·`pendingDescription`까지 그대로 가져왔습니다.

| # | enum | label | 제 더미 (변경 전) |
|---|---|---|---|
| 1 | `PICKED_UP` | 수거 완료 | 접수 완료 ← **순서가 반대였음** |
| 2 | `RECEIVED` | 접수 완료 | 픽업 완료 |
| 3 | `DIAGNOSED` | 진단 및 견적 확정 | 손상 부위 진단 완료 |
| 4 | `REPAIRING` | 수선 진행 중 | 수선 진행 중 |
| 5 | `INSPECTING` | 품질 검수 | 품질 검수 |
| 6 | `SHIPPING` | 반환 배송 | 발송 |
| 7 | `COMPLETED` | 완료 | (없었음) |

제 더미는 "접수 → 픽업" 순이었는데 광은님은 "수거 → (센터 입고)접수" 순입니다. **광은님 쪽이 실제 물류 흐름에 맞습니다.** 제 걸 고쳤습니다.

이제 제 `as_dummy.json`의 각 단계에 enum 코드가 함께 들어갑니다.

```json
{ "status": "REPAIRING", "step": "수선 진행 중", "days_ago": 15, "note": "확정된 범위로 수선 작업 진행" }
```

---

## 2. 🔴 광은님이 하실 것 — 어댑터 한 개

챗봇은 AS 데이터를 지금 `as_dummy.json`에서 읽습니다. 실 DB를 쓰려면 **챗봇이 광은님 API를 호출**하면 됩니다.

`as_chatbot.py`에서 고칠 곳은 함수 **하나**입니다.

```python
# 391행
def find_repair(as_id):
    # 지금: 메모리 리스트 순회
    # 바꿀 것: requests.get(f"{SPRING_URL}/api/asCase/detail/{as_id}") 응답을 매핑
```

`DetailResDto` → 챗봇 필드 매핑은 이렇습니다. **제가 짜겠습니다.** 확인만 부탁드립니다.

| `DetailResDto` | 챗봇 필드 | 비고 |
|---|---|---|
| `asNo` | `as_id` | |
| `modelName` | `product_name` | |
| `status` / `statusLabel` | `status` / `stage` | |
| `createdAt` | `received_at` | `LocalDate` → `"2026년 07월 23일"` 문자열로 변환 필요 |
| `expectedCompletedAt` | `expected_at` | 위와 동일 |
| `statusUpdatedAt` | `updated_at` | |
| `currentLocation` / `locationStatus` | `location` / `location_status` | |
| `intakeType` | `intake_type` | |
| `historyList[].status` / `.statusLabel` | `timeline[].status` / `.step` | |
| `historyList[].occurredAt` | `timeline[].date` | |
| `historyList[].completed` | `timeline[].done` | |
| `historyList[].description` | `timeline[].note` | |

⚠️ **날짜는 이미 포맷된 문자열이어야 합니다.** `LocalDate` 그대로 넘어오면 프롬프트에 `2026-07-23`으로 들어가서 답변이 어색해집니다. 이 변환은 제가 챗봇 쪽에서 하겠습니다.

### 필요한 것 2개

1. **챗봇이 호출할 주소** — 서버가 뜬 base URL
2. **인증** — `/api/asCase/detail/{asNo}`가 JWT를 요구하면, 챗봇이 쓸 방법이 필요합니다. 서버 내부 호출용 토큰이나 IP 화이트리스트 중 편하신 쪽으로 알려주세요.

---

## 3. ⭐ 견적 — 이미 계약이 있어서 붙일 수 있습니다

`EstimatePort` / `EstimateResult`를 보고 알았습니다. **동원님 모델의 input/output이 "미정"이 아니라, 이미 `EstimateResult`가 계약입니다.**

```java
EstimateResult {
  damageCategory, damageSeverity, confidenceGrade, confidenceNote,
  items[] { repairItemName, minPrice, maxPrice }
}
```

`StubEstimateAdapter`가 규칙 기반으로 이미 채우고 있으니, 동원님은 **`ModelEstimateAdapter(@Profile("ai"))` 하나만** 만들면 됩니다. 이거 동원님께 공유하겠습니다 — 지금 "output을 뭘로 줘야 하나" 고민 중이실 것 같습니다.

### 챗봇 쪽 변화

제 챗봇은 지금 **수선 금액을 절대 직답하지 않고** "AI 예상 견적 받기"로 유도합니다. 가격 자료가 없어서 지어내지 않게 막아둔 겁니다.

그런데 `/api/asCase/estimate/{asNo}`가 `totalMinPrice`·`totalMaxPrice`·`itemList`를 주니까, **견적이 이미 나온 건이면 챗봇이 그 금액을 근거를 갖고 설명할 수 있습니다.**

```
고객: "수선비 얼마 나와요?"
지금:  "여기서는 금액 안내가 어려워요. AI 예상 견적 받기에서…"
연동 후: "AS-2026-00871 건은 하드웨어 교체로 8만~12만원이 예상됩니다.
         실물 진단 후 최종 확정됩니다."
```

**Frame 712 '견적 다시 보기' 흐름이 챗봇에서도 이어집니다.** 8/12 회의에서 "AI 견적 결과 화면에 재진입 경로가 없다"고 나왔던 부분인데, 챗봇이 그 경로 하나를 더 만들어 주는 셈입니다.

붙일까요? 제 쪽 작업은 반나절이면 됩니다.

---

## 4. 확인 필요 3개

### ① 멤버십 등급 — `Member`에 필드가 없습니다

제 더미에는 `membership: "GOLD"`가 있는데 `Member` 엔티티에는 등급 필드가 없습니다.

SJF 트랙 심사 기준의 **'영역 정합성 10점'** 항목이 이렇습니다:

> 파트너사가 실제로 보유한 자산(제품 라인업, 매장 공간, 브랜드 헤리티지, **고객 데이터·멤버십 자산**) 중 최소 하나를 솔루션의 핵심 근거로 삼았는가

멤버십을 쓰면 배점에 직접 걸립니다. `Member`에 `grade` 한 컬럼 추가가 부담되실까요? 부담되면 제 챗봇 쪽에서만 표시하고 넘어가겠습니다.

(등급별 할인·우선처리 같은 **혜택 문구는 확정 자료가 없어서 챗봇이 말하지 않도록** 막아뒀습니다. 지어내면 브랜드 톤 정합성에서 감점 위험이 있습니다.)

### ② 로그인 — 뺀다고 했는데 `security` 패키지가 살아 있습니다

8/12 회의 후반부에 요강 근거로 로그인을 빼기로 했는데, JWT·`SecurityConfig`·`AdminKeyFilter`가 그대로 있습니다. **빼는 방향 맞나요?** 챗봇이 API를 호출할 때 인증이 필요한지가 여기 걸려 있습니다.

### ③ 수선 단가표

`StubEstimateAdapter` 주석에 "아래 단가는 임시값"이라고 되어 있습니다. 현황상 **기획에서 확정 단가표가 아직 안 나왔습니다** (동원님 대기). 챗봇이 금액을 말하려면 이게 확정돼야 합니다.

---

## 5. 챗봇 API (프론트 전달용)

| 메서드 | 경로 | 용도 |
|---|---|---|
| POST | `/chat/stream` | **상담 (SSE 스트리밍, 권장)** |
| POST | `/chat` | 상담 (한 번에 응답) |
| GET | `/chat/opening?as_id=` | 첫 인사말 (GPT 미호출, 즉시) |
| DELETE | `/chat/{session_id}` | 대화 초기화 |
| GET | `/health` | 상태 확인 |
| GET | `/docs` | 자동 생성 문서 |

요청 본문 (3개 다 동일):

```json
{ "message": "제 가방 지금 어디에 있어요?",
  "session_id": "demo-1",
  "as_id": "AS-2026-00871" }
```

- `session_id` — 프론트가 만들어 대화 내내 유지하면 맥락이 이어집니다
- `as_id` — 있으면 개인화 상담(Frame 717), 없으면 일반 상담(Frame 718)

`/chat` 응답:

```json
{ "answer": "...", "sources": ["..."], "used_gpt": true,
  "masked": 0, "used_history": false, "session_id": "demo-1" }
```

`/chat/stream` 응답 (SSE):

```
data: {"type":"meta","sources":[...],"used_gpt":true,...}
data: {"type":"delta","text":"고객님의 "}
data: {"type":"done"}
```

⚠️ nginx를 앞에 두시면 `X-Accel-Buffering: no` 헤더를 살려주세요. 버퍼링되면 스트리밍이 무의미해집니다.

> **제 `/repairs`, `/repairs/{as_id}`는 광은님 API와 겹칩니다.** 광은님 `/api/asCase/*`가 정본이고 제 것은 챗봇 자체 테스트용이니, 통합 시 빼겠습니다.

---

## 6. 실행

```bash
pip install fastapi uvicorn openai python-dotenv
uvicorn as_chatbot:app --host 0.0.0.0 --port 8001
```

| 환경변수 | 용도 |
|---|---|
| `OPENAI_API_KEY` | 필수 |
| `MCM_MOCK=1` | **GPT를 안 부르고** 검색·안전장치만 동작. 통합 테스트는 이걸로 하시면 비용·한도를 안 씁니다 |

같은 폴더에 `mcm_products.csv`(상품 지식베이스), `as_dummy.json`(AS 더미)이 있어야 합니다.

---

## 7. 챗봇 안전장치 6개 (건드리면 안 되는 부분)

| # | 장치 |
|---|---|
| 1 | 자료가 없으면 **GPT 호출 자체를 안 함** (환각 차단) |
| 2 | 수선 금액 직답 금지 → 'AI 예상 견적'으로 유도 |
| 3 | GPT 전송 전 개인정보 정규식 치환 (전화·이메일·주민·카드·주소) |
| 4 | 제품 여러 건이면 단정 금지 → 후보 나열 + 되묻기 |
| 5 | API 실패해도 서버 안 죽음 (429·타임아웃 시 대체 문구) |
| 6 | 로그·에러에서 API 키 마스킹 |

4번이 필요한 이유: **이름과 사진이 같고 치수만 다른 상품이 실제로 있습니다.**

```
MMKEAVE12CO001  Stark 사이드 스터드 비세토스 백팩   16 x 33 x 41
MMKEAVE14CO001  스타크 사이드 스터드 비세토스 백팩   13 x 30 x 38   ← 한글 표기만 다름
MMKEAVE01K8001  Stark 비세토스 사이드 스터드 백팩   ← 단어 순서만 다름
```

그래서 챗봇이 상품명만 나열하지 않고 **색상·크기·상품코드를 함께** 보여줍니다.
