# 레포 등록 & 배포 가이드

작성: 2026-08-13 · 챗봇 서버(`as_chatbot.py`) 기준

> ⚠️ **git 명령은 내가 대신 못 돌린다.** 내가 이 폴더를 다루는 통로에서는 파일 삭제가 막혀 있는데, git은 `.git/index.lock`을 만들었다 지우는 걸 반복한다. 그래서 아래는 **네가 터미널에 그대로 붙여넣는** 순서다.
>
> `git init`과 설정(`user.name`/`user.email`)은 이미 해뒀고, 내가 만들다 남긴 잠금 파일도 치웠다. 바로 2번부터 하면 된다.

---

## 1. 크롤링이 끝났는지 먼저 확인

```bash
cd ~/Desktop/mcm_crawler
tail -3 crawler.log
```

`완료: 성공 N건 / 실패 M건` 이 보이면 끝난 것이다. **끝나기 전에 커밋하면 CSV가 잘린 채로 올라간다.**

---

## 2. 올라갈 파일 확인 (가장 중요)

```bash
git status
```

**`.env` 가 목록에 보이면 안 된다.** 보이면 즉시 멈추고 알려줘. — `.gitignore`에 넣어뒀으니 정상이면 안 보인다.

한 번 더 확인하려면:

```bash
git check-ignore -v .env
```

`.gitignore:4:.env	.env` 처럼 나오면 안전하다.

---

## 3. 첫 커밋

```bash
git add .
git commit -m "feat: MCM 케어 AI 상담 챗봇 + 상품 크롤러

- RAG 기반 AS 상담 챗봇 (FastAPI, SSE 스트리밍)
- MCM 공식몰 상품 크롤러 (13개 카테고리)
- 안전장치 6개: 환각 차단, 개인정보 마스킹, 금액 직답 금지 등
- 광은님 Spring 서버 연동 모듈 (spring_client.py)
- 배포 설정: Render / Docker"
```

---

## 4. GitHub 레포 만들기

저장소: **`gyumin0847/MCMcare_Back_AI`** (생성 완료 · Public · 비어 있음)

- 제출 항목에 **"GitHub (AI)"** 칸이 따로 있다 → 여기에 낼 레포다
- 광은님 `MCMcare_Back`과 이름이 이어져서 심사위원이 보기 좋다
- **Public 필수** (제출 요건)
- README·.gitignore·라이선스는 **체크하지 말 것** (이미 있어서 충돌난다)

만든 뒤:

```bash
git remote add origin https://github.com/gyumin0847/MCMcare_Back_AI.git
git branch -M main
git push -u origin main
```

> 평가 기준 브랜치는 **main 또는 master**다. 위 명령이 `main`으로 맞춰준다.

---

## 5. 배포 — Render 추천

가비아 서버 지원은 8/18부터라 그때까지 공백이 생긴다. **프론트가 8/17에 붙이려면 그전에 주소가 있어야 한다.** Render 무료 플랜으로 먼저 띄우고, 가비아는 나중에 옮기거나 병행하면 된다.

`render.yaml`을 이미 만들어뒀다.

1. [render.com](https://render.com) 가입 → **New > Blueprint**
2. `MCMcare_AI` 저장소 선택 → `render.yaml`을 자동으로 읽는다
3. 환경변수 입력 (대시보드에서 직접, 코드엔 안 남는다)

| 키 | 값 |
|---|---|
| `OPENAI_API_KEY` | `.env`에 있는 그 키 |
| `SPRING_API_URL` | 광은님 서버 주소. **모르면 비워둬라** — 비면 더미로 정상 동작한다 |
| `SPRING_API_TOKEN` | 광은님 서버가 JWT를 요구할 때만 |

4. 배포 후 확인:

```bash
curl https://<받은주소>/health
```

```json
{"status":"ok","지식베이스":700,"AS안내":16,"상품":684,"mock모드":false,"API키설정됨":true}
```

이게 나오면 성공이다.

### ⚠️ Render 무료 플랜 주의

**15분 요청이 없으면 서버가 잠든다.** 다시 깨우는 데 30초~1분이 걸린다.

심사위원이 서류 심사 기간(8/21~8/24)에 링크를 눌렀을 때 첫 응답이 1분 걸리면 그대로 감점이다. 대책 두 가지:

- **UptimeRobot**(무료)으로 `/health`를 10분마다 호출해 깨워두기 ← 권장
- 8/18 이후 가비아 서버로 옮기기

제출 요건이 **"행사 종료까지 작동 유지"** 다. 8/25까지 살아 있어야 한다.

---

## 6. 대안 — 가비아 (8/18 이후)

`Dockerfile`을 만들어뒀다. 서버를 받으면:

```bash
docker build -t mcmcare-chatbot .
docker run -d --restart always -p 8000:8000 \
  -e OPENAI_API_KEY=발급받은키 \
  -e SPRING_API_URL=http://광은님서버주소 \
  --name chatbot mcmcare-chatbot
```

`--restart always`가 중요하다. 서버가 재부팅돼도 자동으로 다시 뜬다.

---

## 7. 배포 후 반드시 할 것

### CORS 조이기

지금 `as_chatbot.py`가 `allow_origins=["*"]`다. 개발용이라 **아무 사이트나 이 API를 부를 수 있다.** 프론트 배포 주소가 정해지면 그것만 남겨라.

```python
allow_origins=["https://프론트주소.vercel.app"],
```

### 프론트에 넘길 정보

| 항목 | 값 |
|---|---|
| 챗봇 주소 | 배포 후 받는 URL |
| 상담 (스트리밍) | `POST /chat/stream` |
| 첫 인사말 | `GET /chat/opening?as_id=AS-2026-00871` |
| 대화 초기화 | `DELETE /chat/{session_id}` |
| API 문서 | `GET /docs` |

요청 본문:

```json
{ "message": "제 가방 지금 어디에 있어요?",
  "session_id": "프론트가-만든-값",
  "as_id": "AS-2026-00871" }
```

- `session_id` — 대화 내내 같은 값을 유지하면 맥락이 이어진다
- `as_id` — 있으면 개인화 상담(Frame 717), 없으면 일반 상담(Frame 718)

---

## 8. 체크리스트 (제출 요건)

| | 항목 | 근거 |
|---|---|---|
| ☐ | GitHub **Public** | 제출 필수 |
| ☐ | 브랜치가 `main` | "평가 기준 브랜치는 main 또는 master" |
| ☐ | 배포 URL이 레포와 연동 | "연동 안 된 레포 제출 시 감점" |
| ☐ | 행사 종료(8/25)까지 작동 | "행사 종료까지 작동 유지" |
| ☐ | `.env`가 안 올라감 | 키 유출 시 자동 폐기 |
| ☐ | 기술 스택 **AI 영역** 기재 | "기획·디자인·개발·AI 영역별 1개 이상" |

**마감: 8월 21일(금) 09:59:59** — 이후 수정 적발 시 즉시 탈락.
